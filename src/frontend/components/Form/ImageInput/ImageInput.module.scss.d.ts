export type Styles = {
  cameraIcon: string;
  hasPreview: string;
  imageInput: string;
  imageInputInput: string;
  imagePreview: string;
};

export type ClassNames = keyof Styles;

declare const styles: Styles;

export default styles;
